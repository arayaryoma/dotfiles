interface:
  display_name: "Re-sign Before Push"
  short_description: "Re-sign local commits before push"
  default_prompt: "Detect unsigned local commits and rewrite them as signed commits before pushing."
